version: '2'
services:
    modelbuilder-elasticsearch:
        container_name: modelbuilder-elasticsearch
        image: elasticsearch:2.4.0
        # volumes:
        #     - ~/volumes/jhipster/ModelBuilder/elasticsearch/:/usr/share/elasticsearch/data/
        ports:
            - 9200:9200
            - 9300:9300
